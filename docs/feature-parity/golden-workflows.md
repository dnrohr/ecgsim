# Golden Workflows

Status: feature-parity workflow targets synced with implemented tasks through `0061`. These are test targets, not claims of raw legacy numerical parity.

## Reference Cases

| Case | Path | Role |
| --- | --- | --- |
| Normal male legacy screenshot case | `ECGsim-3.0.1/cases/normal_male.ECGsimcase` | Native-app screenshot and visual reference only; app binary/case directory remains ignored. |
| Normal male archived source case | `research/source/www.ecgsim.org/downloads/cases/normal_male2.ECGsimcase` | Primary modern fixture and first loader/parity case. |
| WPW bundle-only | `research/source/www.ecgsim.org/downloads/cases/WPW_Bundleonly.ECGsimcase` | Multi-case parser and activation variant reference. |
| WPW ectopic beat | `research/source/www.ecgsim.org/downloads/cases/WPW_ectopicbeat.ECGsimcase` | Beat/focus workflow reference. |
| WPW fusion beat | `research/source/www.ecgsim.org/downloads/cases/WPW_fusionbeat.ECGsimcase` | Fusion/variant workflow reference. |

## Workflow Table

| ID | Name | Starting file | Actions | Expected outputs | Validation method | Current blocker |
| --- | --- | --- | --- | --- | --- | --- |
| GW-001 | Open normal case and inspect workspace | `normal_male2.ECGsimcase`; legacy screenshot `normal-male-main-window.png` | Launch app, load the normal case, inspect Heart, Thorax, TMP, and Leads panes. | Case metadata appears; four primary workspaces are visible; heart/thorax geometry, TMP traces, and ECG traces are nonblank. | Browser workflow test plus screenshot review against `research/legacy-exports/screenshots/normal-male-main-window.png`. | No automated pixel/perceptual comparison against the legacy screenshot yet. |
| GW-002 | Open WPW variants | `WPW_Bundleonly.ECGsimcase`, `WPW_ectopicbeat.ECGsimcase`, `WPW_fusionbeat.ECGsimcase` | Use Open case for each WPW file and switch between them without reload. | Case identity, geometry counts, source/beat metadata, and available lead systems update for each file. | Parser regression tests for all WPW cases plus browser file-open workflow for supported bundled cases. | All archived WPW variants are browser-open fixtures; focus editing and exact activation-tool parity remain future work. |
| GW-003 | Select heart node and review TMP | `normal_male2.ECGsimcase` | Click a heart node, adjust selection radius, inspect selected region and TMP plot. | Selected node/region changes in Heart; TMP plot follows the selected source node; invalid selections report status. | Browser interaction test using visible selected node text and nonblank TMP plot; fixture checks against parsed source parameters. | Exact legacy source-node mapping and visual selection contour parity remain unverified. |
| GW-004 | Edit TMP parameter and reset | `normal_male2.ECGsimcase` | Select a node/region, edit depolarization time and amplitude, apply, reset parameter, then reset beat. | Adapted TMP changes after apply; selected parameter reset restores that parameter; reset beat restores all adapted parameters. | Unit tests for edit model plus browser workflow test checking changed/restored values, undo/redo, sidecar save/load, and traces. | Current TMP generator is deterministic but not legacy `.user.source` parity-tested. |
| GW-005 | Switch heart surface functions | `normal_male2.ECGsimcase` | Switch Heart among geometry, depolarization, repolarization, ARI, amplitude, resting potential, and TMP-at-time functions. | Surface coloring and legends update; initial/adapted state is clear; selected node remains stable. | Browser visual/state test plus parser checks for source parameter arrays. | Geometry, depolarization, repolarization, amplitude, and resting potential are implemented; ARI, TMP-at-time, and legacy colormap parity remain incomplete. |
| GW-006 | Navigate thorax BSPM maps | `normal_male2.ECGsimcase` | Switch Thorax among geometry, measured BSPM, initial BSPM, adapted BSPM, sensitivity map; step time and play movie. | BSPM/isofunction maps change with time; scale/legend is visible; lungs/electrodes visibility works. | Browser workflow test for geometry, measured map/time changes, initial/adapted recompute, sensitivity map, scale, lungs, and electrode toggles; future numeric comparison against raw legacy exports. | Initial/adapted BSPM and sensitivity previews are implemented from the transfer candidate; isofunction contours, movie export, and legacy numerical parity remain incomplete. |
| GW-007 | Switch lead systems and overlays | `normal_male2.ECGsimcase`; WPW variants | Select standard lead systems, toggle measured/initial/adapted overlays, adjust scale and grid. | Lead labels, traces, overlay visibility, scale, and grid update without changing the selected case. | Parser tests for lead/electrode metadata plus browser plot-state tests for lead-system, scale, RMS, and grid controls. | Measured/initial/adapted overlay classification and exact lead transform semantics remain unsupported. |
| GW-008 | Validate coupling and filtering modes | `normal_male2.ECGsimcase` | Toggle Baseline, AC, and DC coupling; inspect fiducial-dependent baseline behavior. | Trace baselines and offsets match documented mode semantics; selected lead and time cursor persist. | Unit tests against parsed signals, promoted `standard_12.adaptECG`, derived normal-male fiducials, and browser workflow test for visible mode changes. | Normal-male fiducials are derived from export evidence; decoded P/T fiducials remain unavailable for arbitrary `.ECGsimcase` inputs. |
| GW-009 | Use linked time cursor | `normal_male2.ECGsimcase` | Drag/step the time cursor in Leads/TMP/Thorax; use arrow keys and playback. | Time marker is synchronized across ECG, TMP, Heart TMP-at-time, and Thorax BSPM views. | Browser workflow test over TMP, Leads, Thorax map time, keyboard input, and playback state. | Heart TMP-at-time surface mode and movie-frame parity remain incomplete. |
| GW-010 | Probe thorax contribution/sensitivity | `normal_male2.ECGsimcase` | Select a thorax node or electrode, enable probe/sensitivity/contribution display. | Thorax selection is visible; Heart contribution/sensitivity map updates; selected ECG target is identified. | Browser workflow test for thorax selection plus future numeric comparison from transfer matrix data. | Contribution/sensitivity visualization and confirmed transfer-matrix roles remain missing. |
| GW-011 | Create or edit focus/activation | WPW cases | Open Focus tools, inspect or edit activation focus, opposite wall, and propagation velocity where supported. | Focus data is visible/editable; activation/source changes propagate to TMP and ECG after recompute. | Parser tests for activation/focus data, route-solver tests, viewer smoke tests, and browser workflow checks. | WPW activation records are visible in the TMP Focus controls and selected nodes can drive a route preview. Opposite-wall mapping, raw activation-field writes, and propagation into TMP/ECG remain future work. |
| GW-012 | Export and reload adapted work | `normal_male2.ECGsimcase` | Make a small TMP edit, export directory, save sidecar/case, reload output, and compare values. | Exported ECG/TMP/source/electrode/triangulation files are present; reloaded adaptation preserves edits. | File-level export regression tests, sidecar browser workflow tests, capture validation tools, promoted legacy fixture scenario tests, and future MATLAB/readECGsim compatibility smoke where possible. | Supported export subset and sidecar exist; electrodes, adapted ECG export recomputation, and full legacy export-directory reproduction remain missing. |
| GW-013 | Copy visual output | `normal_male2.ECGsimcase` | Copy or download Heart, Thorax, TMP, and Leads views; export movie where supported. | Images are nonblank, include current view state, and can be opened outside the app. | Browser PNG download checks for all primary panes plus best-effort clipboard implementation. | Movie export and legacy clipboard parity remain missing. |
| GW-014 | Open help and references | Any bundled case | Open About, references, case-download link, and validation/status help. | Version/build data, scientific references, source links, and known limitations are visible. | Docs, static package manifest, and release-validation evidence exist; future browser navigation test should cover an in-app Help/About view. | In-app Help/About/reference view remains missing. |

## Promotion Rules

A workflow can become `parity-tested` only when all of these are true:

- The starting case is committed or explicitly documented as an ignored local legacy artifact.
- The action sequence is automated or recorded as a repeatable manual script.
- Expected outputs are stated as observable UI state, file output, or numeric values.
- The validation method identifies the reference artifact and tolerance from `docs/parity.md`.

Until then, workflows should be treated as planning anchors for tasks `0028` through `0061`.
